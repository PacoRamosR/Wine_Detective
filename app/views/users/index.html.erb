<% provide(:title, 'All users') %>

<div class="index-dark-overlay">
    <div class="home-inner container pb-5 mb-5">
        <h1>All users</h1>
        <%= paginate @users %>
        <%= render @users %>
        <%= paginate @users %>
    </div>
</div>
